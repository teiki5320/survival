name: train_cosy
description: "Side-scroller train, cosy post-apocalyptic narrative game."
publish_to: "none"
version: 0.99.144+348

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/background/
    - assets/cinematic/
    - assets/objects/
    - assets/characters/
    - assets/audio/
    - assets/plants/
